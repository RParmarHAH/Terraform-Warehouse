CREATE OR REPLACE PROCEDURE DW_PROD.HAH.DELETE_STAGE_CCSI_DIM_CONTRACT()
RETURNS VARCHAR(16777216)
LANGUAGE JAVASCRIPT
EXECUTE AS OWNER
AS '
  sqlCmd = `DELETE FROM DW_PROD.HAH.DIM_CONTRACT WHERE SOURCE_SYSTEM_ID =8 AND CONTRACT_KEY \\
  NOT IN (SELECT CONTRACT_KEY FROM DW_PROD.STAGE.CCSI_DIM_CONTRACT)`;
  sqlStmt = snowflake.createStatement( {sqlText: sqlCmd} );
  rs = sqlStmt.execute();
  return ''Done'';
';