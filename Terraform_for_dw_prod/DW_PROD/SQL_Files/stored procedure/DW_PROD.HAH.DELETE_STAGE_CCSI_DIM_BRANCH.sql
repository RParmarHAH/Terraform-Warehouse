CREATE OR REPLACE PROCEDURE DW_PROD.HAH.DELETE_STAGE_CCSI_DIM_BRANCH()
RETURNS VARCHAR(16777216)
LANGUAGE JAVASCRIPT
EXECUTE AS OWNER
AS '
  sqlCmd = `DELETE FROM DW_PROD.HAH.DIM_BRANCH WHERE SOURCE_SYSTEM_ID =8 AND BRANCH_KEY \\
  NOT IN (SELECT BRANCH_KEY FROM DW_PROD.STAGE.CCSI_DIM_BRANCH)`;
  sqlStmt = snowflake.createStatement( {sqlText: sqlCmd} );
  rs = sqlStmt.execute();
  return ''Done'';
';