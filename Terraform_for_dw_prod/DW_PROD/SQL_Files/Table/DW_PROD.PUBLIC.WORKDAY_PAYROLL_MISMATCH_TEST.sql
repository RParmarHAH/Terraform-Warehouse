CREATE OR REPLACE TABLE DW_PROD.PUBLIC.WORKDAY_PAYROLL_MISMATCH_TEST (
	EMPLOYEE_KEY VARCHAR(32),
	OLD_ID NUMBER(38,0),
	WORKER VARCHAR(50),
	PERIODSTARTDATE DATE,
	PERIODENDDATE DATE,
	HOURSWORKED2 NUMBER(38,3)
);