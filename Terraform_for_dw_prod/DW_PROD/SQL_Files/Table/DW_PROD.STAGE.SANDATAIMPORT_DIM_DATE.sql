CREATE OR REPLACE TABLE DW_PROD.STAGE.SANDATAIMPORT_DIM_DATE (
	CALENDAR_DATE DATE NOT NULL,
	DATE_KEY NUMBER(38,0) NOT NULL,
	YEAR NUMBER(38,0) NOT NULL,
	QUARTER NUMBER(38,0) NOT NULL,
	YEAR_QUARTER VARCHAR(5) NOT NULL,
	MONTH NUMBER(38,0) NOT NULL,
	YEAR_MONTH VARCHAR(6) NOT NULL,
	MONTH_NAME VARCHAR(3) NOT NULL,
	DAY_OF_MONTH NUMBER(38,0) NOT NULL,
	DAY_OF_WEEK NUMBER(38,0) NOT NULL,
	DAY_NAME VARCHAR(9) NOT NULL,
	WEEK_OF_YEAR NUMBER(38,0) NOT NULL,
	DAY_OF_YEAR NUMBER(38,0) NOT NULL,
	YEAR_DAY VARCHAR(7) NOT NULL,
	WEEKEND_FLAG BOOLEAN NOT NULL
);