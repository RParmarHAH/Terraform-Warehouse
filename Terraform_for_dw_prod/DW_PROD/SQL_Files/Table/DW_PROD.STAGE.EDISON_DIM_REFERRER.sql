CREATE OR REPLACE TABLE DW_PROD.STAGE.EDISON_DIM_REFERRER (
	REFERRER_KEY VARCHAR(32) NOT NULL,
	SOURCE_SYSTEM_ID NUMBER(38,0) NOT NULL,
	SYSTEM_CODE VARCHAR(20) NOT NULL,
	REFERRER_TYPE VARCHAR(30) COMMENT 'Agency\nDoctor\nMedical practitioner\n????',
	REFERRER_CODE VARCHAR(10),
	REFERRER_NAME VARCHAR(100) NOT NULL,
	REFERRER_ADDRESS1 VARCHAR(150),
	REFERRER_ADDRESS2 VARCHAR(150),
	REFERRER_CITY VARCHAR(50),
	REFERRER_STATE_CODE VARCHAR(2),
	REFERRER_ZIP VARCHAR(12),
	REFERRER_PHONE VARCHAR(20),
	REFERRER_FAX VARCHAR(20),
	DESCRIPTION VARCHAR(2000),
	ETL_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_LAST_UPDATED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_INSERTED_BY VARCHAR(100) NOT NULL,
	ETL_LAST_UPDATED_BY VARCHAR(100) NOT NULL,
	ETL_DELETED_FLAG BOOLEAN NOT NULL,
	constraint PK_REFERRER_KEY primary key (REFERRER_KEY)
);