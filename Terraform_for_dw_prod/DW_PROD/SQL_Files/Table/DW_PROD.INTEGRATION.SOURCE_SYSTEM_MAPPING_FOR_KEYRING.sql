CREATE OR REPLACE TABLE DW_PROD.INTEGRATION.SOURCE_SYSTEM_MAPPING_FOR_KEYRING (
	SOURCE_SYSTEM_ID NUMBER(38,0),
	SOURCE_SYSTEM_NAME VARCHAR(50),
	AMS_COST_CENTER_HIERARCHY VARCHAR(200),
	STATE VARCHAR(10),
	SYSTEM_CODE VARCHAR(50),
	LOCATION_HIERARCHY VARCHAR(50),
	SYSTEM_NAME VARCHAR(51)
);