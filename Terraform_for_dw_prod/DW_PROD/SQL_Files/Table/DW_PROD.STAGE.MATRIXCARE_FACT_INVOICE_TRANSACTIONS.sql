CREATE OR REPLACE TABLE DW_PROD.STAGE.MATRIXCARE_FACT_INVOICE_TRANSACTIONS (
	TRANSACTION_KEY VARCHAR(32) NOT NULL,
	TRANSACTION_NUMBER VARCHAR(38) NOT NULL,
	REVENUE_KEY VARCHAR(32) NOT NULL,
	VISIT_KEY VARCHAR(32) NOT NULL,
	TRANSACTION_TYPE_KEY VARCHAR(32) NOT NULL,
	AMOUNT NUMBER(38,2),
	TRANSACTION_CREATED DATE,
	TRANSACTION_POSTED DATE,
	TRANSACTION_DATE DATE,
	FINAL_TRANSACTION_DATE DATE,
	CHECK_NUMBER VARCHAR(52),
	CHECK_DATE DATE,
	DEPOSIT_DATE DATE,
	POSTED_DATE DATE,
	SYSTEM_CODE VARCHAR(20) NOT NULL,
	SOURCE_SYSTEM_ID NUMBER(38,0) NOT NULL,
	ETL_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_INSERTED_BY VARCHAR(100) NOT NULL,
	ETL_LAST_UPDATED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_LAST_UPDATED_BY VARCHAR(100) NOT NULL,
	ETL_DELETED_FLAG BOOLEAN NOT NULL,
	primary key (TRANSACTION_KEY)
);