CREATE OR REPLACE TABLE DW_PROD.REPORT.HOURS_AND_CENSUS_OVERTIME_HOURS (
	PAYROLL_DATE DATE,
	MONTH_BEGIN_DATE DATE,
	MONTH_DISPLAY_NAME VARCHAR(100),
	BRANCH_KEY VARCHAR(32),
	STATE VARCHAR(2),
	OFFICE_NUMBER NUMBER(38,3),
	BRANCH_NAME VARCHAR(200),
	EMPLOYEE_KEY VARCHAR(32) NOT NULL,
	EMPLOYEE_ID VARCHAR(32),
	SERVICE_HOURS NUMBER(38,3),
	OVERTIME_HOURS NUMBER(38,3),
	OVERTIME_PAY_AMOUNT NUMBER(38,3),
	ETL_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_INSERTED_BY VARCHAR(100) NOT NULL,
	ETL_LAST_UPDATED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_LAST_UPDATED_BY VARCHAR(100) NOT NULL,
	ETL_DELETED_FLAG BOOLEAN NOT NULL,
	constraint FK_HOURS_AND_CENSUS_OVERTIME_HOURS_STATE foreign key (STATE) references DW_PROD.HAH.DIM_STATE(STATE_ISO_CODE)
);