CREATE OR REPLACE TABLE DW_PROD.HAH.DIM_COMPANY_STATE (
	COMPANY_KEY VARCHAR(32) NOT NULL,
	COMPANY_ID NUMBER(38,0) NOT NULL,
	COMPANY_LEGAL_NAME VARCHAR(50) NOT NULL,
	COMPANY_COMMON_NAME VARCHAR(10) NOT NULL,
	FEIN VARCHAR(15) NOT NULL,
	STATE_ISO_CODE VARCHAR(2) NOT NULL,
	CURRENT_SOURCE_SYSTEM_ID NUMBER(38,0),
	CURRENT_SYSTEM_CODE VARCHAR(20),
	PAYROLL_SOURCE_SYSTEM_ID NUMBER(38,0),
	PAYROLL_SYSTEM_CODE VARCHAR(20),
	PREVIOUS_SOURCE_SYSTEM_ID NUMBER(38,0),
	PREVIOUS_SYSTEM_CODE VARCHAR(20),
	constraint PK_COMPANY_STATE primary key (COMPANY_KEY)
);