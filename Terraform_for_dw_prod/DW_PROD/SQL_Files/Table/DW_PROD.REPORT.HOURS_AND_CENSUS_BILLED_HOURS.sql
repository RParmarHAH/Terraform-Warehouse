CREATE OR REPLACE TABLE DW_PROD.REPORT.HOURS_AND_CENSUS_BILLED_HOURS (
	PERIOD_BEGIN_DATE DATE NOT NULL,
	PERIOD_NAME VARCHAR(100) NOT NULL,
	STATE VARCHAR(2) NOT NULL,
	BRANCH_KEY VARCHAR(32) NOT NULL,
	OFFICE_NUMBER NUMBER(38,0) NOT NULL,
	OFFICE_NAME VARCHAR(100) NOT NULL,
	SUPERVISOR_KEY VARCHAR(32),
	SUPERVISOR_CODE VARCHAR(100),
	CLIENT_KEY_DATA VARCHAR(32) NOT NULL,
	CLIENT_NUMBER VARCHAR(50) NOT NULL,
	CONTRACT_KEY VARCHAR(32) NOT NULL,
	CONTRACT_CODE VARCHAR(40) NOT NULL,
	CLIENT_KEY VARCHAR(32),
	HOURS_SERVED_WHERE_AUTH_EXISTS NUMBER(38,3),
	HOURS_BILLED_WHERE_AUTH_EXISTS NUMBER(38,3),
	HOURS_AUTHORIZED_WHERE_AUTH_EXISTS NUMBER(38,3),
	HOURS_SERVED_ALL NUMBER(38,3),
	HOURS_BILLED_ALL NUMBER(38,3),
	HOURS_AUTHORIZED_ALL NUMBER(38,3),
	INCLUDE_FOR_CLIENTS BOOLEAN NOT NULL,
	INCLUDE_FOR_HOURS BOOLEAN NOT NULL,
	REVENUE_CATEGORY VARCHAR(10) NOT NULL,
	ETL_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_INSERTED_BY VARCHAR(100) NOT NULL,
	ETL_LAST_UPDATED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_LAST_UPDATED_BY VARCHAR(100) NOT NULL,
	ETL_DELETED_FLAG BOOLEAN NOT NULL,
	constraint PK_HOURS_AND_CENSUS_BILLEDHOURS_PERIODBEGINDATE_STATE_CLIENTKEYDATA_CONTRACTKEY primary key (PERIOD_BEGIN_DATE, STATE, CLIENT_KEY_DATA, CONTRACT_KEY)
);