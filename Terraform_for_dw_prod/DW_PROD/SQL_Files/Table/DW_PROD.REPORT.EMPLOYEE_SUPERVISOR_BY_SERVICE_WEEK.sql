CREATE OR REPLACE TABLE DW_PROD.REPORT.EMPLOYEE_SUPERVISOR_BY_SERVICE_WEEK (
	SERVICE_WEEK DATE,
	EMPLOYEE_KEY VARCHAR(32),
	BRANCH_KEY VARCHAR(32),
	BRANCH_STATE VARCHAR(2),
	BRANCH_NAME VARCHAR(100),
	EMPLOYEE_SYSTEM_CODE VARCHAR(20),
	EMPLOYEE_ID VARCHAR(50),
	EMPLOYEE_LAST_NAME VARCHAR(50),
	EMPLOYEE_CATEGORY VARCHAR(15),
	SUPERVISOR_KEY VARCHAR(32),
	SUPERVISOR_SYSTEM_CODE VARCHAR(20),
	SUPERVISOR_CODE VARCHAR(50),
	SUPERVISOR_NAME VARCHAR(100),
	ETL_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_INSERTED_BY VARCHAR(100) NOT NULL,
	ETL_LAST_UPDATED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_LAST_UPDATED_BY VARCHAR(100) NOT NULL,
	ETL_DELETED_FLAG BOOLEAN NOT NULL
);