CREATE OR REPLACE TABLE DW_PROD.HAH.FACT_SYSTEM_CUTOVER_DATE (
	SOURCE_SYSTEM_ID NUMBER(38,0) NOT NULL,
	SYSTEM_CODE VARCHAR(20) NOT NULL,
	OFFICE_STATE_CODE VARCHAR(2),
	BRANCH_KEY VARCHAR(32),
	CONTRACT_KEY VARCHAR(32),
	START_DATE DATE,
	END_DATE DATE,
	constraint UQ_FACT_SYSTEM_CUTOVER_DATE_BRANCH_KEY unique (BRANCH_KEY, CONTRACT_KEY)
);