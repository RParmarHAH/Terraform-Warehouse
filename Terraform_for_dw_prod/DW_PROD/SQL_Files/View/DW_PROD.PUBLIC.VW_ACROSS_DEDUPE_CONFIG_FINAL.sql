create or replace view DW_PROD.PUBLIC.VW_ACROSS_DEDUPE_CONFIG_FINAL(
	ID,
	SOURCESYSTEM,
	SOURCETYPE,
	SOURCE,
	TASK_NAME,
	TASK_GROUP,
	CODE_FILE,
	DEDUPE_TYPE,
	DEDUPE_OUTPUT_FILE,
	CONFIG,
	RULE_TYPE,
	PRIMARY_SOURCE_DATABASE_NAME,
	PRIMARY_SOURCE_SCHEMA_NAME,
	PRIMARY_SOURCE_TABLE_NAME,
	DESTINATION_DATABASE_NAME,
	DESTINATION_SCHEMA_NAME,
	DESTINATION_TABLE_NAME,
	CONTAINER_NAME,
	DIRECTORY_NAME,
	LOADTYPE,
	FLAG,
	RUNTYPEFLAG,
	LASTRUNDATE,
	INSERTED_DATE,
	INSERTED_BY,
	UPDATED_BY,
	TEMP_STATUES_CHECK,
	RULES
) as
select C.*,R.RULES from DW_PROD.PUBLIC.ACROSS_DEDUPE_CONFIG C 
LEFT JOIN DEDUPE_STAGE_LAYER.CONFIGURATION.RULES_CONFIG R ON C.RULE_TYPE =R.RULE_TYPE;