create or replace view DW_PROD.STAGE.VW_CURRENT_MATRIXCARE_DIM_SERVICES_1_0(
	SERVICE_KEY
) as
SELECT 
MD5('MATRIXCARE' || '-' || SVCC_ID || '-' || 'MATRIXCARE') AS SERVICE_KEY
FROM DISC_PROD.MATRIXCARE.STVHC_T_SERVICECODE
WHERE ETL_LAST_UPDATED_DATE >= '1900-01-01';