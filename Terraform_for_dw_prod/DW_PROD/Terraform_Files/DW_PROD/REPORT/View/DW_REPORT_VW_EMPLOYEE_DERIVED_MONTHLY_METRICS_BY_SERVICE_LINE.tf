resource "snowflake_view" "DW_REPORT_VW_EMPLOYEE_DERIVED_MONTHLY_METRICS_BY_SERVICE_LINE" {
	database = "DW_${var.SF_ENVIRONMENT}"
	schema = "REPORT"
	name = "VW_EMPLOYEE_DERIVED_MONTHLY_METRICS_BY_SERVICE_LINE"
	statement = <<-SQL
	 SELECT * FROM REPORT.EMPLOYEE_DERIVED_MONTHLY_METRICS_BY_SERVICE_LINE
WHERE SERVICE_MONTH BETWEEN DATE_TRUNC('YEAR',DATEADD('YEAR',-2,CURRENT_DATE)) AND CURRENT_DATE;
SQL
	or_replace = true 
	is_secure = false 
}

