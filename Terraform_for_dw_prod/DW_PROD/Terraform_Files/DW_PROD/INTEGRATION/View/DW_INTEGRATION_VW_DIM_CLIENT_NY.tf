resource "snowflake_view" "DW_INTEGRATION_VW_DIM_CLIENT_NY" {
	database = "DW_${var.SF_ENVIRONMENT}"
	schema = "INTEGRATION"
	name = "VW_DIM_CLIENT_NY"
	statement = <<-SQL
	 SELECT TRIM(CLIENT_KEY) ID,SYSTEM_CODE,CLIENT_PID,CLIENT_DOB,CLIENT_NAME,CLIENT_FIRST_NAME,CLIENT_LAST_NAME,CLIENT_ADDRESS1,CLIENT_CITY,CLIENT_HOME_PHONE  from DW_${var.SF_ENVIRONMENT}.HAH.DIM_CLIENT WHERE SOURCE_SYSTEM_ID = 17 AND SYSTEM_CODE IN ('PREFERRED','EDISON');
SQL
	or_replace = true 
	is_secure = false 
}

