CREATE OR REPLACE TABLE DISC_PROD.MATRIXCARE.MATRIXCARE_MASTER_EMPLOYEE_MAPPING_ALL (
	EMPLOYEE_KEY VARCHAR(32),
	EMPLOYEE_NUMBER NUMBER(19,0),
	EMPLOYEE_DOB DATE,
	EMPLOYEE_FIRST_NAME VARCHAR(150),
	EMPLOYEE_LAST_NAME VARCHAR(150),
	EMPLOYEE_ADDRESS_1 VARCHAR(765),
	EMPLOYEE_ADDRESS_2 VARCHAR(180),
	EMPLOYEE_CITY VARCHAR(150),
	EMPLOYEE_STATE_CODE VARCHAR(60),
	EMPLOYEE_ZIP VARCHAR(20),
	EMPLOYEE_HOME_PHONE VARCHAR(30),
	EMPLOYEE_CELL_PHONE VARCHAR(30),
	EMPLOYEE_WORK_PHONE VARCHAR(30),
	EMPLOYEE_PERSONAL_EMAIL VARCHAR(150),
	MASTER_EMPLOYEE_KEY VARCHAR(100),
	MASTER_EMPLOYEE_NUMBER VARCHAR(100),
	ETL_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_TASK_KEY NUMBER(38,0) NOT NULL,
	ETL_INSERTED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_INSERTED_BY VARCHAR(100) NOT NULL,
	ETL_LAST_UPDATED_DATE TIMESTAMP_NTZ(9) NOT NULL,
	ETL_LAST_UPDATED_BY VARCHAR(100) NOT NULL,
	ETL_DELETED_FLAG BOOLEAN NOT NULL,
	constraint PK_EMPLOYEE_KEY primary key (EMPLOYEE_KEY)
);