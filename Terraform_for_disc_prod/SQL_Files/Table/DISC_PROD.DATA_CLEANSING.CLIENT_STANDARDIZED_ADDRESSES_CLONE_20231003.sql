CREATE OR REPLACE TABLE DISC_PROD.DATA_CLEANSING.CLIENT_STANDARDIZED_ADDRESSES_CLONE_20231003 (
	SOURCE_SYSTEM_ID NUMBER(38,0) NOT NULL,
	SYSTEM_CODE VARCHAR(20) NOT NULL,
	CLIENT_NUMBER VARCHAR(50) NOT NULL,
	CLIENT_FIRST_NAME VARCHAR(100) COMMENT 'First name of client',
	CLIENT_LAST_NAME VARCHAR(100) COMMENT 'Last name of client',
	ADDRESS VARCHAR(1000),
	CITY VARCHAR(500),
	STATE VARCHAR(100),
	ZIP VARCHAR(20),
	POINTS VARCHAR(256),
	LATITUDE FLOAT,
	LONGITUDE FLOAT,
	LABEL VARCHAR(256),
	ADDRESS_NUMBER VARCHAR(10),
	STREET VARCHAR(256),
	MUNICIPALITY VARCHAR(256),
	REGION VARCHAR(256),
	SUB_REGION VARCHAR(256),
	COUNTRY_CODE VARCHAR(256),
	ZIP_CODE VARCHAR(10),
	ZIP_PLUS_FOUR VARCHAR(10),
	RELEVANCE FLOAT
);