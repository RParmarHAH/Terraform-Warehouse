CREATE OR REPLACE TABLE DISC_PROD.STAGE.ADAPTIVE_CONTRACTCODE_MAPPING (
	OFFCENO NUMBER(38,0),
	OFFICENAME VARCHAR(100),
	CONTRACTCODE VARCHAR(20),
	CONTRACTNAME VARCHAR(100),
	BILLCODE VARCHAR(20),
	DFDESCRIPTION VARCHAR(100),
	PAYORID NUMBER(38,0),
	SERVICETYPE VARCHAR(100),
	LASTWEEKEND DATE
);